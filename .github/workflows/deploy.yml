name: Deploy Node.js App

on:
  push:
    branches: ['master']

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "📥 Go to project folder..."
            cd moto_blog

            echo "📥 Cope folder from old build..."
            rm -r ~/tmp/.output

            echo "📥 Cope folder from old build..."
            cp -r .output/ ~/tmp/

            echo "📥 Remove folder from old build..."
            rm -r .output node_modules

            echo "📥 Pulling latest changes..."
            git pull origin master

            echo "📦 Installing dependencies..."
            npm install

            echo "📦 Start building..."
            npm run build

            echo "📦 Restart PM2..."
            pm2 restart pro_moto_blog

            echo "✅ Deployment complete!"

      - name: Send Telegram notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: |
            Статус: ${{ job.status == 'success' && '✅ Success' || '❌ Fail' }}
            Репозиторий: moto_blog
            Сообщение: ${{ github.event.head_commit.message }}
            Ветка: ${{ github.ref_name }}
            Автор: ${{ github.actor }}
